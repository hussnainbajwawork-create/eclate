// Re-export DB helpers from /lib/db. Legacy file kept for back-compat imports.
export { formatPKR } from "./format";
export type { Product, Category, ProductColor, ProductImage, StockStatus } from "./db";
